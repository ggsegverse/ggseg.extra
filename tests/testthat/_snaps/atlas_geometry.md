# extract_contours / logs progress when verbose is TRUE

    Code
      invisible(extract_contours(input_dir, output_dir, verbose = TRUE))
    Message
      i  Extracting contours
      v  Extracting contours [<time>]
      

