% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subcortical_builders.R
\name{subcortical_slabs}
\alias{subcortical_slabs}
\alias{subcortical_views}
\title{Build subcortical slabs from a label bounding box}
\usage{
subcortical_slabs(
  volume,
  labels,
  coronal = 0,
  axial = 0,
  sagittal = 0,
  pad = 0,
  reorient = TRUE
)

subcortical_views(
  volume,
  labels,
  coronal = 0,
  axial = 0,
  sagittal = 0,
  pad = 0,
  reorient = TRUE
)
}
\arguments{
\item{volume}{Path to a label volume, or an integer array already in the
builder's frame.}

\item{labels}{Integer label ids whose combined bounding box frames the
slabs.}

\item{coronal, axial, sagittal}{Number of slabs to produce for each
orientation (\code{0} = none).}

\item{pad}{Voxels by which to expand the bounding box before slabbing.}

\item{reorient}{Passed to the internal volume reader; must match the value
\code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}} uses (default \code{TRUE}).}
}
\value{
A data.frame with columns \code{name}, \code{type}, \code{start}, \code{end}.
}
\description{
Computes evenly spaced coronal, axial and/or sagittal slabs spanning the
bounding box of the requested labels, ready to pass as the \code{slabs}
argument of \code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}.

\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}

\code{subcortical_views()} was renamed to \code{\link[=subcortical_slabs]{subcortical_slabs()}} to match the
\code{slabs} argument of \code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}} and
\code{\link[=create_tract_from_tractography]{create_tract_from_tractography()}}.
}
\details{
The volume is read with the \strong{same} axis reorientation the builder uses
internally, so the returned slab indices are in the projection frame. This
avoids a subtle trap: \code{RNifti::readNifti()} and the builder's reader can
return different axis orders, so a bounding box computed from the RNifti
array points the slabs at the wrong slices (silently producing empty
views). Always derive slabs with this function rather than indexing the
volume by hand.
}
\examples{
vol <- array(0L, dim = c(20, 20, 20))
vol[8:12, 6:14, 9:11] <- 17L
subcortical_slabs(vol, labels = 17, coronal = 3, axial = 2)
}
\seealso{
\code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}, \code{\link[=aseg_context]{aseg_context()}}
}
