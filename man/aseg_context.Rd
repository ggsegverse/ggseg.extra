% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subcortical-builders.R
\name{aseg_context}
\alias{aseg_context}
\title{Reduce a subcortical atlas to focus regions on grey anatomical context}
\usage{
aseg_context(
  atlas,
  focus,
  match_on = c("label", "region"),
  remove = aseg_hidden_labels(),
  punch_white_matter = TRUE,
  cortex = "^cortex",
  white_matter = "White-Matter$",
  drop_empty_views = TRUE
)
}
\arguments{
\item{atlas}{A subcortical \code{ggseg_atlas}, e.g. from
\code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}.}

\item{focus}{Regex matched (case-insensitively) against \code{match_on}
identifying the regions to keep as coloured core; everything else becomes
context.}

\item{match_on}{Column to match \code{focus} against: \code{"label"} or \code{"region"}.}

\item{remove}{Character vector of \code{\link[ggseg.formats:atlas_manipulation]{ggseg.formats::atlas_region_remove()}}
patterns to strip before demoting context. Defaults to
\code{\link[=aseg_hidden_labels]{aseg_hidden_labels()}}; pass \code{character(0)} to skip.}

\item{punch_white_matter}{If \code{TRUE}, subtract \code{white_matter} from the
\code{cortex} silhouette. Skipped with a message if either pattern is absent.}

\item{cortex, white_matter}{Label patterns for the brain silhouette and the
cerebral white matter used by the punch.}

\item{drop_empty_views}{If \code{TRUE}, remove views containing no focus region.}
}
\value{
The post-processed \code{ggseg_atlas}.
}
\description{
Applies the post-processing recipe shared by the FreeSurfer subcortical
atlases:
}
\details{
\enumerate{
\item Punch the cerebral white matter out of the brain silhouette
(\code{\link[ggseg.formats:atlas_manipulation]{ggseg.formats::atlas_region_op()}}) so slices show the cortical ribbon
with the white-matter interior as a hole rather than a solid blob.
\item Remove the structures \code{aseg} does not draw (\code{\link[=aseg_hidden_labels]{aseg_hidden_labels()}}).
\item Demote every remaining structure that is \strong{not} matched by \code{focus} to
grey context, so only the focus regions are coloured.
\item Optionally drop views left with no focus geometry.
}

The context demotion matches the leftover core labels exactly and
case-sensitively, so a focus region is never swallowed by a context entry
that happens to be a substring of its name (e.g. \code{Thalamus} vs
\code{hypothalamus}).
}
\seealso{
\code{\link[=subcortical_views]{subcortical_views()}}, \code{\link[ggseg.formats:atlas_manipulation]{ggseg.formats::atlas_region_contextual()}}
}
