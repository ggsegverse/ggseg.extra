% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/github_actions.R
\name{use_atlas_github_actions}
\alias{use_atlas_github_actions}
\title{Add ggsegverse GitHub Actions workflows to a package}
\usage{
use_atlas_github_actions(
  workflows = atlas_github_actions(),
  path = ".",
  overwrite = FALSE
)
}
\arguments{
\item{workflows}{Names of workflows to add. Defaults to all of the above.}

\item{path}{Path to the package. Defaults to the working directory.}

\item{overwrite}{Overwrite workflows that already exist. Default is FALSE,
which leaves existing files untouched and reports them.}
}
\value{
Invisibly, the paths written.
}
\description{
Writes caller stubs for the shared workflows maintained in
\href{https://github.com/ggsegverse/.github}{ggsegverse/.github} into
\verb{.github/workflows/}. Each stub is a short \verb{uses:} block, so a package
picks up changes to the shared workflow without needing to be updated
itself.
}
\details{
Run this on a newly scaffolded atlas package, or on an existing one to
replace hand-maintained workflows with the shared set.
\code{\link[=setup_atlas_repo]{setup_atlas_repo()}} calls it for you unless \code{github_actions = FALSE}.

The workflows written by default are:
\itemize{
\item \code{R-CMD-check} — multi-platform \verb{R CMD check}
\item \code{code-quality} — air, lintr, and goodpractice
\item \code{pkgdown} — build and deploy the documentation site
\item \code{render-readme} — re-render \code{README.qmd} on change
\item \code{update-codemeta} — refresh \code{codemeta.json} when DESCRIPTION changes
}
}
\examples{
\dontrun{
# Add the full set to the package in the working directory
use_atlas_github_actions()

# Add just one, replacing any existing copy
use_atlas_github_actions("pkgdown", overwrite = TRUE)
}
}
