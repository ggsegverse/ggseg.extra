% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tract_from_volume.R
\name{create_tract_from_volume}
\alias{create_tract_from_volume}
\title{Create a white matter tract atlas from a label volume}
\usage{
create_tract_from_volume(
  input_volume,
  input_lut,
  input_aseg = NULL,
  exclude = NULL,
  n_points = 50L,
  min_voxels = 30L,
  smoother = "smooth_spline",
  atlas_name = NULL,
  output_dir = NULL,
  verbose = get_verbose(),
  ...
)
}
\arguments{
\item{input_volume}{Path or \code{RNifti} image of the tract label volume.}

\item{input_lut}{Path to a colour lookup table, or a data.frame with \code{idx},
\code{label} (or \code{region}) and colour columns (\code{R}, \code{G}, \code{B}). Supplies tract
names and colours; labels absent from the volume are ignored.}

\item{input_aseg}{Path to a segmentation volume in the same space, used to
draw the grey-brain cortex outline in the 2D views. Required for the 2D
projection (see \code{steps}).}

\item{exclude}{Integer label ids to drop (for example aggregate whole-brain
fibre masks). Labels with fewer than \code{min_voxels} voxels, or for which a
centerline cannot be fit, are dropped automatically with a message.}

\item{n_points}{Number of points along each tract centerline.}

\item{min_voxels}{Minimum voxel count for a tract to be kept.}

\item{smoother}{Principal-curve smoother, passed to
\code{\link[princurve:principal_curve]{princurve::principal_curve()}}.}

\item{atlas_name}{Name for the atlas. If NULL, derived from the input
filename.}

\item{output_dir}{Directory to store intermediate files (screenshots, masks,
contours). Defaults to \code{\link[=tempdir]{tempdir()}}.}

\item{verbose}{Verbosity level: \code{0} (silent), \code{1} (standard progress,
default), or \code{2} (debug, includes FreeSurfer output). Logical values
are accepted (\code{TRUE} = 1, \code{FALSE} = 0). If not specified, uses the
value from \code{options("ggseg.extra.verbose")} or the \code{GGSEG_EXTRA_VERBOSE}
environment variable.}

\item{...}{Passed to \code{\link[=create_tract_from_tractography]{create_tract_from_tractography()}} (for example
\code{tube_radius}, \code{tube_segments}, \code{steps}).}
}
\value{
A \code{ggseg_atlas} of type \code{"tract"}, as returned by
\code{\link[=create_tract_from_tractography]{create_tract_from_tractography()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Build a tract atlas from a volumetric white-matter tract \emph{label map} — one
integer label per tract — rather than from streamlines. Each tract's voxel
cloud is reduced to an ordered centerline with a principal curve, and the
centerlines are handed to \code{\link[=create_tract_from_tractography]{create_tract_from_tractography()}}, which builds
the 3D tubes and 2D projection. This suits probabilistic tract atlases
distributed as NIfTI label volumes (e.g. AtlasTrack).
}
\examples{
\dontrun{
atlas <- create_tract_from_volume(
  input_volume = "AtlasTrack_labels.nii.gz",
  input_lut = "AtlasTrack_LUT.txt",
  input_aseg = "fsaverage/mri/aseg.mgz",
  exclude = c(2000, 2001, 2002, 2003, 2004),
  tube_radius = 3
)
}
}
\seealso{
\code{\link[=create_tract_from_tractography]{create_tract_from_tractography()}} for the streamline-based
counterpart.
}
