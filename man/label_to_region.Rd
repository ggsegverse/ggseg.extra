% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_atlas.R
\name{label_to_region}
\alias{label_to_region}
\title{Derive an atlas \code{region} from a label}
\usage{
label_to_region(label_name, remove_hemi = TRUE, normalize = TRUE)
}
\arguments{
\item{label_name}{Character vector of labels.}

\item{remove_hemi}{Strip hemisphere affixes (default \code{TRUE}).}

\item{normalize}{Lower-case and convert separators to spaces
(default \code{TRUE}).}
}
\value{
A character vector of region names.
}
\description{
The rule every atlas pipeline here uses to fill the \code{region} column: strip
the hemisphere affix, turn brackets, hyphens, underscores and slashes into
spaces, lower-case the result and squeeze runs of whitespace.
}
\details{
Exported because atlas build scripts need to reproduce it exactly. A script
that adds a \code{name} column keyed on \code{region}, for instance, has to key on
what the pipeline will actually derive; re-implementing the rule by hand is
how a key silently stops matching (an underscore handled but a hyphen not,
and one region joins to \code{NA}).

The affixes stripped here must match the ones \code{detect_hemi()} recognises.
Where they disagree the hemisphere ends up in \code{region} as well as \code{hemi}:
\code{detect_hemi()} reads the \code{L_}/\code{R_} convention, so \code{R_Fx} correctly gave
hemi \code{"right"}, while this function left the prefix in place and produced
region \code{"r fx"}. Two hemispheres of one structure then look like two
different structures, since \code{region} is what pairs them.
}
\examples{
label_to_region("Left-Thalamus")
label_to_region("Central_Lateral-Lateral_Posterior_Left")
label_to_region(c("Pu_Left", "SNc_PBP_VTA_Right"))
}
