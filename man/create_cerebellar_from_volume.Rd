% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-cerebellar.R
\name{create_cerebellar_from_volume}
\alias{create_cerebellar_from_volume}
\title{Create cerebellar atlas from volume segmentation}
\usage{
create_cerebellar_from_volume(
  volume,
  input_lut = NULL,
  atlas_name = NULL,
  output_dir = NULL,
  tolerance = NULL,
  smooth_refinements = NULL,
  decimate = 0.5,
  cleanup = NULL,
  verbose = get_verbose(),
  skip_existing = NULL
)
}
\arguments{
\item{volume}{Path to a cerebellar segmentation volume (NIfTI).}

\item{input_lut}{Optional path to a colour lookup table file, or a
data.frame with columns \code{idx}, \code{label}, and optionally \code{R}, \code{G}, \code{B}.
If NULL, labels are auto-generated from volume values.}

\item{atlas_name}{Name for the atlas. If NULL, derived from the input
filename.}

\item{output_dir}{Directory to store intermediate files (screenshots, masks,
contours). Defaults to \code{\link[=tempdir]{tempdir()}}.}

\item{tolerance}{Simplification tolerance for 2D polygons. Higher values
produce simpler shapes with fewer vertices (typical range: 0.1--2).
Passed to \code{\link[sf:geos_unary]{sf::st_simplify()}}.
If not specified, uses \code{options("ggseg.extra.tolerance")} or the
\code{GGSEG_EXTRA_TOLERANCE} environment variable. Default is 1.}

\item{smooth_refinements}{Number of Chaikin corner-cutting refinements
to apply to 2D polygons. Higher values produce smoother region
boundaries (typical range: 0--3). 0 disables smoothing.
If not specified, uses \code{options("ggseg.extra.smooth_refinements")} or the
\code{GGSEG_EXTRA_SMOOTH_REFINEMENTS} environment variable. Default is 2.}

\item{decimate}{Mesh decimation factor between 0 and 1. Reduces the number
of faces in 3D meshes using quadric edge decimation (via
\code{\link[Rvcg:vcgQEdecim]{Rvcg::vcgQEdecim()}}). A value of 0.5 reduces faces by 50\%. Set to NULL
to skip decimation. Requires the Rvcg package. Default is 0.5.}

\item{cleanup}{Remove intermediate files after atlas creation. If not
specified, uses \code{options("ggseg.extra.cleanup")} or the \code{GGSEG_EXTRA_CLEANUP}
environment variable. Default is TRUE.}

\item{verbose}{Verbosity level: \code{0} (silent), \code{1} (standard progress,
default), or \code{2} (debug, includes FreeSurfer output). Logical values
are accepted (\code{TRUE} = 1, \code{FALSE} = 0). If not specified, uses the
value from \code{options("ggseg.extra.verbose")} or the \code{GGSEG_EXTRA_VERBOSE}
environment variable.}

\item{skip_existing}{Skip generating output files that already exist,
allowing interrupted atlas creation to resume. If not specified, uses
\code{options("ggseg.extra.skip_existing")} or the \code{GGSEG_EXTRA_SKIP_EXISTING}
environment variable. Default is TRUE.}
}
\value{
A \code{ggseg_atlas} object of type "cerebellar" with both sf geometry
and 3D meshes.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Creates a cerebellar atlas from a NIfTI segmentation volume. Uses the
bundled SUIT 3D surface for volume-to-surface sampling and the SUIT flatmap
for 2D polygon generation. Per-region 3D meshes are tessellated from the
volume for 3D rendering.
}
\examples{
\dontrun{
atlas <- create_cerebellar_from_volume(
  volume = "cerebellar_parcellation.nii.gz"
)
}
}
