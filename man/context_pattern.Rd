% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_atlas.R
\name{context_pattern}
\alias{context_pattern}
\title{The label pattern that matches an atlas's brain silhouette}
\usage{
context_pattern()
}
\value{
A regular expression, as a length-one character vector.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The grey outline drawn behind an atlas's structures is labelled \code{cortex},
and almost every polish step treats it differently from the structures --
a silhouette wants rounding that keeps its sulci, the structures want
their staircase taken off. That means writing the same pattern twice per
build, in every build, and the pattern has to agree with what the
pipelines actually label it.

This is that pattern, in one place. Use it rather than retyping
\code{"^cortex"}, so a change of convention is one release rather than one edit
per atlas repository.
}
\details{
Anchored and case-sensitive on purpose. A loose \code{"cortex"} also matches
\verb{Cerebellar_Cortex_*} and \code{Left-Cerebral-Cortex}, which are structures, not
the silhouette.
}
\examples{
# The usual shape of a build: silhouette and structures, each its own way.
\dontrun{
atlas <- my_atlas |>
  atlas_polish(
    keep = 0.4,
    smoothness = 0.4,
    method = "chaikin",
    labels = context_pattern()
  ) |>
  atlas_polish(keep = 0.1, smoothness = 0.4, exclude = context_pattern())
}
}
