% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freesurfer.R
\name{mri_surf2surf_rereg}
\alias{mri_surf2surf_rereg}
\title{Re-register an annotation file}
\usage{
mri_surf2surf_rereg(
  subject,
  annot,
  hemisphere = c("lh", "rh"),
  target_subject = "fsaverage5",
  output_dir = as.character(fs::path(freesurfer::fs_subj_dir(), subject, "label")),
  verbose = get_verbose(),
  hemi = lifecycle::deprecated()
)
}
\arguments{
\item{subject}{subject the original annotation file is registered to}

\item{annot}{annotation file name (as found in subjects_dir)}

\item{hemisphere}{hemisphere (one of "lh" or "rh")}

\item{target_subject}{subject to re-register the annotation
(default fsaverage5)}

\item{output_dir}{Directory to store intermediate files (screenshots, masks,
contours). Defaults to \code{\link[=tempdir]{tempdir()}}.}

\item{verbose}{Verbosity level: \code{0} (silent), \code{1} (standard progress,
default), or \code{2} (debug, includes FreeSurfer output). Logical values
are accepted (\code{TRUE} = 1, \code{FALSE} = 0). If not specified, uses the
value from \code{options("ggseg.extra.verbose")} or the \code{GGSEG_EXTRA_VERBOSE}
environment variable.}

\item{hemi}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{hemisphere} instead.}
}
\value{
nothing
}
\description{
Annotation files are subject specific.
Most are registered for fsaverage, but
we recommend using fsaverage5 for the mesh
plots in ggseg3d, as these contain a decent
balance in number of vertices for detailed
rendering and speed.
}
\examples{
\dontrun{
# For help see:
freesurfer::fs_help("mri_surf2surf")

mri_surf2surf_rereg(
  subject = "bert",
  annot = "aparc.DKTatlas",
  target_subject = "fsaverage5"
)
}
}
