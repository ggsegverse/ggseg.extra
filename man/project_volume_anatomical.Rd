% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coregister-volume.R
\name{project_volume_anatomical}
\alias{project_volume_anatomical}
\title{Project atlas labels onto FreeSurfer anatomical context}
\usage{
project_volume_anatomical(
  input_volume,
  label_ids = NULL,
  lut = NULL,
  registration = NULL,
  target_subject = "cvs_avg35_inMNI152",
  threshold = 0.3,
  id_offset = 200L,
  protect_cortex = TRUE,
  output_file = NULL,
  subjects_dir = freesurfer::fs_subj_dir(),
  verbose = get_verbose()
)
}
\arguments{
\item{input_volume}{Path to the atlas volume, or an \code{RNifti} object.}

\item{label_ids}{Integer vector of labels to project. Defaults to all
non-zero labels in \code{input_volume}.}

\item{lut}{Optional colour LUT (data frame with at least an \code{idx}
column, or path to a TSV with \verb{idx, label, R, G, B, A}). If
provided, returned alongside the volume with \code{idx} shifted by
\code{id_offset} to match.}

\item{registration}{Path to an LTA file (typically from
\code{\link[=coregister_volume]{coregister_volume()}}). If \code{NULL}, \code{mri_vol2vol} falls back to
\code{--regheader} and trusts the volume's xform.}

\item{target_subject}{FreeSurfer subject providing the anatomical grid
and \code{aparc+aseg.mgz}. Defaults to \code{"cvs_avg35_inMNI152"}.}

\item{threshold}{Numeric in \verb{[0, 1]}. Voxels whose argmax probability
does not exceed this threshold are kept as the source \code{aparc+aseg}
label. Defaults to \code{0.3}.}

\item{id_offset}{Integer added to every input label ID when writing
the merged volume to avoid collisions with FreeSurfer \code{aparc+aseg}
labels. Defaults to \code{200L}. Set to \code{0L} if you have already remapped
your IDs (or if you've verified there are no collisions).}

\item{protect_cortex}{Logical. If \code{TRUE} (default), cortex voxels in
\code{aparc+aseg} (aparc labels \code{1000-2999}, plus cortical white matter
\code{2} and \code{41}) are never overwritten by user labels, even when argmax
wins above \code{threshold}. This preserves the brain-outline geometry
that the subcortical pipeline renders as context. Disable only if
you intentionally want user labels to overwrite cortex.}

\item{output_file}{Path for the merged volume. Defaults to a temp file.}

\item{subjects_dir}{FreeSurfer \code{SUBJECTS_DIR}. Defaults to
\code{\link[freesurfer:fs_dir]{freesurfer::fs_subj_dir()}}.}

\item{verbose}{Verbosity level: \code{0} (silent), \code{1} (standard progress,
default), or \code{2} (debug, includes FreeSurfer output). Logical values
are accepted (\code{TRUE} = 1, \code{FALSE} = 0). If not specified, uses the
value from \code{options("ggseg.extra.verbose")} or the \code{GGSEG_EXTRA_VERBOSE}
environment variable.}
}
\value{
If \code{lut} is \code{NULL}, the path to the merged anatomical-context
volume (invisibly), with the \code{id_offset} used recorded as an
attribute. If \code{lut} is supplied, a list with \code{output_file}, \code{lut}
(the input LUT with \code{idx} shifted by \code{id_offset}), and \code{id_offset}.
}
\description{
For each label in an atlas volume, resamples a binary indicator with
trilinear interpolation onto the target FreeSurfer subject's
\code{aparc+aseg} grid, takes the argmax across labels at every voxel, and
returns a merged volume that combines the source \code{aparc+aseg}
(providing anatomical brain-outline context) with the user's atlas
labels (replacing \code{aparc+aseg} voxels wherever a label wins above
\code{threshold}).
}
\details{
The merged volume is what
\code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}
needs to render 2D slices that show real brain outlines around the
atlas regions, instead of a generic shape.

Atlas IDs that collide with FreeSurfer \code{aparc+aseg} labels (e.g. an
atlas where \code{11} means "Putamen" while FS uses \code{11} for "Caudate")
would cause the subcortical pipeline to extract leftover \code{aparc+aseg}
voxels of a different anatomical structure as if they belonged to the
user's region. To prevent this, the function shifts every input label
ID by \code{id_offset} (default \code{200}) when writing the merged volume, so
that the user's IDs sit in a range that doesn't overlap any
\code{aparc+aseg} label. The \code{lut} argument is shifted in the same way so
it matches the merged volume.
}
\examples{
\dontrun{
lta <- coregister_volume("atlas.nii.gz")
merged <- project_volume_anatomical(
  "atlas.nii.gz",
  label_ids = c(11, 12, 13, 17, 18, 26),
  registration = lta
)
}
}
\seealso{
\code{\link[=coregister_volume]{coregister_volume()}}, \code{\link[=prepare_subcortical_anatomical]{prepare_subcortical_anatomical()}}
}
