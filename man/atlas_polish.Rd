% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_geometry.R
\name{atlas_polish}
\alias{atlas_polish}
\title{Simplify and smooth an atlas against a vertex budget}
\usage{
atlas_polish(
  atlas,
  keep = 0.1,
  smoothness = 0.4,
  method = c("close", "chaikin", "ksmooth", "spline"),
  close_gaps = TRUE,
  labels = NULL,
  exclude = NULL
)
}
\arguments{
\item{atlas}{A \code{ggseg_atlas}.}

\item{keep}{Proportion of the original vertices to aim for. See details.}

\item{smoothness}{Smoothing strength between 0 and 1, passed to
\code{\link[=atlas_smooth]{atlas_smooth()}}.}

\item{method}{Smoothing method, passed to \code{\link[=atlas_smooth]{atlas_smooth()}}. \code{"close"}
rounds solid shapes; the others keep holes open.}

\item{close_gaps}{Whether to hand back the slivers the operations open
between neighbouring regions. See \code{\link[=atlas_smooth]{atlas_smooth()}}.}

\item{labels}{Optional regex. Only matching labels are polished.}

\item{exclude}{Optional regex. Matching labels are left alone.}
}
\value{
The \code{ggseg_atlas}, simplified and rounded.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The two things a finished atlas usually needs at once: fewer vertices, and
its voxel staircase rounded off. Doing them separately means deciding which
order they go in, and the answer is not obvious - rounding \emph{adds} vertices,
so smoothing after simplifying undoes some of the reduction, while
simplifying after smoothing replaces the new curves with straight chords
and puts the staircase back.

\code{atlas_polish()} owns that order so a build does not have to rediscover it.
}
\details{
\code{keep} is a budget on the result, not on an intermediate: the atlas is
simplified to \code{keep} of the vertices it arrived with, then rounded under
\code{\link[=atlas_smooth]{atlas_smooth()}}'s \code{"preserve"} budget, which simplifies the rounding back
towards that same count.

It is a dial rather than a guarantee, and the low end is the loose end.
Simplification will not take a ring below the handful of vertices that
holds its shape, so an atlas of many small rings lands well above what was
asked. On \code{ggsegJHU}'s tract atlas (14,667 vertices):\tabular{llll}{
   \code{keep} \tab asked \tab \code{\link[=atlas_simplify]{atlas_simplify()}} alone \tab \code{atlas_polish()} \cr
   0.05 \tab 733 \tab 2,636 \tab 3,989 \cr
   0.10 \tab 1,467 \tab 3,341 \tab 4,890 \cr
   0.20 \tab 2,933 \tab 4,729 \tab 6,437 \cr
   0.50 \tab 7,334 \tab 8,570 \tab 10,417 \cr
}


Most of that gap is the floor, not the rounding: \code{\link[=atlas_simplify]{atlas_simplify()}} on its
own misses the same target the same way. Rounding then adds roughly a third
more, which the \code{"preserve"} budget takes back only as far as the floor
allows. Ask for what you want, then look at what you got.
}
\examples{
\dontrun{
# The usual shape of a build: the context silhouette and the structures
# want different budgets, so they get a call each.
atlas <- my_atlas |>
  atlas_polish(
    keep = 0.4,
    smoothness = 0.4,
    method = "chaikin",
    labels = "^cortex"
  ) |>
  atlas_polish(keep = 0.1, smoothness = 0.4, exclude = "^cortex")
}
}
\seealso{
\code{\link[=atlas_simplify]{atlas_simplify()}} and \code{\link[=atlas_smooth]{atlas_smooth()}} for the separate steps,
when a build needs to interleave them differently.

Other atlas geometry:
\code{\link[=atlas_dilate]{atlas_dilate()}},
\code{\link[=atlas_simplify]{atlas_simplify()}},
\code{\link[=atlas_smooth]{atlas_smooth()}}
}
\concept{atlas geometry}
