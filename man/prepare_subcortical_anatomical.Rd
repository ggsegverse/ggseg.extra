% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coregister-volume.R
\name{prepare_subcortical_anatomical}
\alias{prepare_subcortical_anatomical}
\title{Prepare an atlas for the subcortical pipeline with anatomical context}
\usage{
prepare_subcortical_anatomical(
  input_volume,
  label_ids = NULL,
  lut = NULL,
  target_subject = "cvs_avg35_inMNI152",
  target_volume = "brain",
  threshold = 0.3,
  id_offset = 200L,
  protect_cortex = TRUE,
  dof = 12,
  output_file = NULL,
  output_lta = NULL,
  binarise = TRUE,
  subjects_dir = freesurfer::fs_subj_dir(),
  skip_existing = FALSE,
  verbose = get_verbose()
)
}
\arguments{
\item{input_volume}{Path to the atlas volume to coregister, or an
\code{RNifti} object.}

\item{label_ids}{Integer vector of labels to project. Defaults to all
non-zero labels in \code{input_volume}.}

\item{lut}{Optional colour LUT (data frame with at least an \code{idx}
column, or path to a TSV with \verb{idx, label, R, G, B, A}). If
provided, returned alongside the volume with \code{idx} shifted by
\code{id_offset} to match.}

\item{target_subject}{FreeSurfer subject name to register to. Defaults
to \code{"cvs_avg35_inMNI152"}, which is in MNI152 1mm space.}

\item{target_volume}{Name of the volume in the subject's \verb{mri/}
directory used as the registration target. Defaults to \code{"brain"}
(i.e. \code{brain.mgz}).}

\item{threshold}{Numeric in \verb{[0, 1]}. Voxels whose argmax probability
does not exceed this threshold are kept as the source \code{aparc+aseg}
label. Defaults to \code{0.3}.}

\item{id_offset}{Integer added to every input label ID when writing
the merged volume to avoid collisions with FreeSurfer \code{aparc+aseg}
labels. Defaults to \code{200L}. Set to \code{0L} if you have already remapped
your IDs (or if you've verified there are no collisions).}

\item{protect_cortex}{Logical. If \code{TRUE} (default), cortex voxels in
\code{aparc+aseg} (aparc labels \code{1000-2999}, plus cortical white matter
\code{2} and \code{41}) are never overwritten by user labels, even when argmax
wins above \code{threshold}. This preserves the brain-outline geometry
that the subcortical pipeline renders as context. Disable only if
you intentionally want user labels to overwrite cortex.}

\item{dof}{Degrees of freedom for \code{mri_coreg} (\code{6}, \code{9}, or \code{12}).
Defaults to \code{12} (rigid + per-axis scale + shear).}

\item{output_file}{Path for the merged volume. Defaults to a temp file.}

\item{output_lta}{Path to write the resulting LTA file. Defaults to a
temporary file.}

\item{binarise}{Logical. If \code{TRUE} (default), binarise both volumes
before coregistration. Set to \code{FALSE} to register on raw intensities.}

\item{subjects_dir}{FreeSurfer \code{SUBJECTS_DIR}. Defaults to
\code{\link[freesurfer:fs_dir]{freesurfer::fs_subj_dir()}}.}

\item{skip_existing}{Skip generating output files that already exist,
allowing interrupted atlas creation to resume. If not specified, uses
\code{options("ggseg.extra.skip_existing")} or the \code{GGSEG_EXTRA_SKIP_EXISTING}
environment variable. Default is TRUE.}

\item{verbose}{Verbosity level: \code{0} (silent), \code{1} (standard progress,
default), or \code{2} (debug, includes FreeSurfer output). Logical values
are accepted (\code{TRUE} = 1, \code{FALSE} = 0). If not specified, uses the
value from \code{options("ggseg.extra.verbose")} or the \code{GGSEG_EXTRA_VERBOSE}
environment variable.}
}
\value{
Path to the merged anatomical-context volume (invisibly).
}
\description{
Convenience wrapper that runs \code{\link[=coregister_volume]{coregister_volume()}} followed by
\code{\link[=project_volume_anatomical]{project_volume_anatomical()}} in one call, producing a merged volume
on a FreeSurfer subject's \code{aparc+aseg} grid that's ready to feed
\code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}.
}
\examples{
\dontrun{
merged <- prepare_subcortical_anatomical(
  input_volume = "shen_2mm_268_parcellation.nii.gz",
  label_ids = subcortical_ids
)
atlas <- create_subcortical_from_volume(
  input_volume = merged,
  input_lut = subcortical_lut
)
}
}
