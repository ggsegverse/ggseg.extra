% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_anatomy.R
\name{lut_classify_anatomy}
\alias{lut_classify_anatomy}
\title{Classify lookup-table labels as cortical, subcortical, or cerebellar}
\usage{
lut_classify_anatomy(
  volume,
  lut,
  subject = "cvs_avg35_inMNI152",
  min_cortical = 0.6,
  min_cerebellar = 0.5,
  min_fraction = 0.05,
  verbose = get_verbose()
)
}
\arguments{
\item{volume}{Path to the labelled atlas volume (\code{.nii}, \code{.nii.gz} or
\code{.mgz}), in the space its header claims.}

\item{lut}{A lookup table: a path to a LUT file, or a data.frame with
\code{idx} and \code{label} columns. Any existing \code{type} column is replaced.}

\item{subject}{FreeSurfer subject to take the \code{aparc+aseg} from. The
default sits in MNI152 space, which is where most published
parcellations are distributed.}

\item{min_cortical}{Minimum share of a label's labelled grey matter that
must be cortical ribbon for the label to be called cortical.}

\item{min_cerebellar}{Minimum share of a label's labelled grey matter
that must be cerebellar cortex for the label to be called cerebellar.}

\item{min_fraction}{Minimum share of a label's \emph{own voxels} that must sit
on the winning tissue, whatever the grey-matter share says. This is what
stops one ribbon voxel from making a white-matter label cortical.}

\item{verbose}{Verbosity level: \code{0} (silent), \code{1} (standard progress,
default), or \code{2} (debug, includes FreeSurfer output). Logical values
are accepted (\code{TRUE} = 1, \code{FALSE} = 0). If not specified, uses the
value from \code{options("ggseg.extra.verbose")} or the \code{GGSEG_EXTRA_VERBOSE}
environment variable.}
}
\value{
The lookup table as a data.frame, with a \code{type} column of
\code{"cortical"}, \code{"subcortical"} or \code{"cerebellar"}. Labels the volume does
not carry are typed \code{"subcortical"}, matching how
\code{\link[=create_wholebrain_from_volume]{create_wholebrain_from_volume()}} treats a label that never reaches the
surface, and warned about. The background label \code{idx = 0} is left
\code{NA}.
}
\description{
Fills in a lookup table's \code{type} column by reading where each label sits
in FreeSurfer's \code{aparc+aseg}, rather than by matching label names. This is
an authoring tool: run it once while building an atlas, write the \code{type}
column it returns into the lookup table, and commit that. A declared
classification is reviewable in a diff and reproducible without
FreeSurfer; the vertex-count fallback in
\code{\link[=create_wholebrain_from_volume]{create_wholebrain_from_volume()}} is neither.
}
\details{
\code{aparc+aseg} is resampled onto the volume's own grid with
\code{mri_vol2vol --regheader --nearest}, which goes through the two headers
and invents no transform. Each label is then judged on the share of the
\emph{labelled grey matter} it touches - cortical ribbon, deep grey,
cerebellar cortex or brainstem - ignoring white matter and the voxels
\code{aparc+aseg} does not label at all. That normalisation is what makes the
test independent of a label's size: a volume clustered on EPI data
reaches past the edge of FreeSurfer's brain, so a superficial parcel can
be mostly unlabelled and still unambiguously cortical in the grey it does
touch.

Cerebellum is separated first, because a label straddling the tentorium
reads as part cortical. A label that touches no labelled grey matter at
all, and one whose winning tissue is under \code{min_fraction} of its own
voxels, are both called subcortical: the share alone would let a single
stray ribbon voxel decide an otherwise unlabelled label.
}
\examples{
\dontrun{
# Authoring an atlas: classify once, then commit the column.
lut <- read_lut("julich_LUT.txt")
lut <- lut_classify_anatomy("julich_mpm.nii.gz", lut)
table(lut$type)
write_lut(lut, "julich_LUT.txt")

# create_wholebrain_from_volume() then reads the column instead of
# falling back to counting surface vertices.
create_wholebrain_from_volume(
  input_volume = "julich_mpm.nii.gz",
  input_lut = "julich_LUT.txt"
)
}
}
\seealso{
\code{\link[=create_wholebrain_from_volume]{create_wholebrain_from_volume()}}, which consumes the \code{type}
column; \code{\link[=read_lut]{read_lut()}} and \code{\link[=write_lut]{write_lut()}} to read and write the table.
}
