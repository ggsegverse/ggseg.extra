% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coregister-volume.R
\name{coregister_volume}
\alias{coregister_volume}
\title{Coregister an atlas volume to a FreeSurfer subject}
\usage{
coregister_volume(
  input_volume,
  target_subject = "cvs_avg35_inMNI152",
  target_volume = "brain",
  output_lta = NULL,
  dof = 12,
  binarise = TRUE,
  subjects_dir = freesurfer::fs_subj_dir(),
  skip_existing = FALSE,
  verbose = get_verbose()
)
}
\arguments{
\item{input_volume}{Path to the atlas volume to coregister, or an
\code{RNifti} object.}

\item{target_subject}{FreeSurfer subject name to register to. Defaults
to \code{"cvs_avg35_inMNI152"}, which is in MNI152 1mm space.}

\item{target_volume}{Name of the volume in the subject's \verb{mri/}
directory used as the registration target. Defaults to \code{"brain"}
(i.e. \code{brain.mgz}).}

\item{output_lta}{Path to write the resulting LTA file. Defaults to a
temporary file.}

\item{dof}{Degrees of freedom for \code{mri_coreg} (\code{6}, \code{9}, or \code{12}).
Defaults to \code{12} (rigid + per-axis scale + shear).}

\item{binarise}{Logical. If \code{TRUE} (default), binarise both volumes
before coregistration. Set to \code{FALSE} to register on raw intensities.}

\item{subjects_dir}{FreeSurfer \code{SUBJECTS_DIR}. Defaults to
\code{\link[freesurfer:fs_dir]{freesurfer::fs_subj_dir()}}.}

\item{skip_existing}{Skip generating output files that already exist,
allowing interrupted atlas creation to resume. If not specified, uses
\code{options("ggseg.extra.skip_existing")} or the \code{GGSEG_EXTRA_SKIP_EXISTING}
environment variable. Default is TRUE.}

\item{verbose}{Verbosity level: \code{0} (silent), \code{1} (standard progress,
default), or \code{2} (debug, includes FreeSurfer output). Logical values
are accepted (\code{TRUE} = 1, \code{FALSE} = 0). If not specified, uses the
value from \code{options("ggseg.extra.verbose")} or the \code{GGSEG_EXTRA_VERBOSE}
environment variable.}
}
\value{
Path to the LTA file (invisibly).
}
\description{
Computes a rigid-plus-scale registration from a volumetric atlas onto
a FreeSurfer subject's T1 grid (e.g. \code{cvs_avg35_inMNI152}) using
\code{mri_coreg}. The resulting LTA can be reused by \code{mri_vol2vol} to
resample the atlas (or any volume in the same source space) onto the
target's grid, which is the first step toward producing subcortical
atlases that show realistic brain-outline anatomical context in their
2D slices.
}
\details{
Both volumes are binarised (any non-zero voxel becomes brain) before
coregistration so that the alignment is driven by tissue extent rather
than label values. To skip the binarisation and align using raw
intensities, set \code{binarise = FALSE}.
}
\examples{
\dontrun{
lta <- coregister_volume(
  input_volume = "shen_2mm_268_parcellation.nii.gz",
  target_subject = "cvs_avg35_inMNI152"
)
}
}
\seealso{
\code{\link[=project_volume_anatomical]{project_volume_anatomical()}},
\code{\link[=prepare_subcortical_anatomical]{prepare_subcortical_anatomical()}}
}
